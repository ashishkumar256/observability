apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: elasticsearch
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}